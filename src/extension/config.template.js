/**
 * StealthBrowser Engine — config template.
 *
 * The extension builder (src/main/services/extensionBuilder.ts) replaces
 * this file with the real per-profile values and writes it as `config.js`
 * into the temporary extension directory. The MAIN-world script receives
 * the same values inline (__INJECT_CONFIG__), so pages never see this file.
 */
window.__STEALTH_CONFIG__ = __INJECT_CONFIG__;
